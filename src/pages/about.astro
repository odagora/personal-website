---
import { AboutDetails } from '@/partials';
import Base from '@/templates/Base.astro';
import { AppConfig } from '@/utils/AppConfig';

const { title, description } = AppConfig;
---

<Base head={{ title, description }}>
  <AboutDetails />
</Base>
